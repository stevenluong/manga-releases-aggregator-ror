<!DOCTYPE html>
<html>
<head>
  <title>RailsApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<ul>
		<li>Slapps MRA</li>
		<li><%=link_to "Last Releases",mangas_path%></li>
    <li><%=link_to user_signed_in? ? "Subscriptions":"Subscribe !",subscriptions_path%></li>
		<%if !user_signed_in?%>
			<li><%= link_to 'Sign in', new_user_session_path %></li>
			<li><%= link_to "Sign up",new_user_registration_path%></li>
		<%else%>
			<li>Logged as <%=current_user.email%> <%=link_to "Sign Out",destroy_user_session_path,:method=>:delete%></li>
		<%end%>
	</ul>
	<hr/>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
